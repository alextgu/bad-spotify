# --- core ---
pydantic>=2.7
pyyaml>=6.0
python-dotenv>=1.0

# --- agent graph ---
langgraph>=0.2.0
langchain-core>=0.3.0

# --- models (all optional at runtime; mock backends exist) ---
google-genai>=0.3.0          # Gemini: perception + judge
elevenlabs>=1.0              # narrator voice
transformers>=4.46           # local CLIP mood classifier
torch>=2.2                   # local model runtime
Pillow>=10.0                 # image input for Transformers
# twelvelabs                 # optional: post-session recap only, NOT the live loop

# --- capture ---
opencv-python>=4.9
sounddevice>=0.4.6
numpy>=1.26
librosa>=0.10                # local audio features (tempo, onsets, centroid)

# --- music ---
spotipy>=2.23                # playback control only (audio-features is dead)
requests>=2.31
beautifulsoup4>=4.12         # everynoise genre-map scrape

# --- the Gradio surface (app.py) ---
gradio>=4.44

# --- tests ---
pytest>=8.0

# --- hud ---
fastapi>=0.110
python-multipart>=0.0.9
uvicorn[standard]>=0.29
websockets>=12.0
